# Solane Run public edge has no runtime secrets.
#
# API and bot environment files live in their own private/runtime locations:
# - /srv/solane-run/shared/solane-run.env
# - /srv/solane-run/shared/solane-bot.env
